<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- The Basics -->
    <groupId>br.com</groupId>
    <artifactId>store-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>war</packaging>

    <!-- More Project Information -->
    <name>STORE-BACKEND</name>
    <description>STORE-BACKEND</description>
    <url></url>

    <organization>
        <name>Store</name>
        <url></url>
    </organization>

    <mailingLists>
        <mailingList>
            <name>rafael</name>
            <post>rafael_morelo@yahoo.com.br</post>
        </mailingList>
    </mailingLists>

    <properties>
        <!-- Environment -->
        <runtime.home>/opt/UOLI/webapps/${project.artifactId}/</runtime.home>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jdk.version>1.7</jdk.version>

        <sonar.java.coveragePlugin>jacoco</sonar.java.coveragePlugin>
        <jacoco.version>0.7.0.201403182114</jacoco.version>
        <sonar.dynamicAnalysis>reuseReports</sonar.dynamicAnalysis>
        <sonar.jacoco.reportPath>${project.build.directory}/jacoco-ut.exec</sonar.jacoco.reportPath>
        <sonar.jacoco.itReportPath>${project.build.directory}/jacoco-it.exec</sonar.jacoco.itReportPath>
        <sonar.host.url>http://integracao.plataforma.intranet/sonar/</sonar.host.url>
        <sonar.jdbc.url>jdbc:mysql://localhost:3306/sonar</sonar.jdbc.url>
        <sonar.language>java</sonar.language>
        
        <!-- Dependencies Versions -->

        <dependency.version.aspectj>1.8.4</dependency.version.aspectj>
        <dependency.version.commonslang>3.3.2</dependency.version.commonslang>
        <dependency.version.commonsbeanutils>1.9.2</dependency.version.commonsbeanutils>
        <dependency.version.joda>2.5</dependency.version.joda>
        <dependency.version.spring>4.0.5.RELEASE</dependency.version.spring>
        <dependency.version.spring-data>1.6.0.RELEASE</dependency.version.spring-data>
        <dependency.version.hibernate>4.3.1.Final</dependency.version.hibernate>
        <dependency.version.hibernate.validator>5.1.2.Final</dependency.version.hibernate.validator>
        <dependency.version.javax.el.api>2.2.4</dependency.version.javax.el.api>
		<dependency.version.javax.el>2.2.4</dependency.version.javax.el>
		<dependency.version.hibernate.validator.cdi>5.1.3.Final</dependency.version.hibernate.validator.cdi>
		<dependency.version.jersey.bean.validation>2.14</dependency.version.jersey.bean.validation>
        <dependency.version.ojdbc>11.2.0.2.0</dependency.version.ojdbc>
        <dependency.version.bonecp>0.8.0.RELEASE</dependency.version.bonecp>
        
        <dependency.version.validationAPI>0.0.1</dependency.version.validationAPI>


        
        <dependency.version.perf4j>0.9.16</dependency.version.perf4j>
        <dependency.version.junit>4.12</dependency.version.junit>
        <dependency.version.mockito>1.9.5</dependency.version.mockito>
        <dependency.version.jmockit>1.7</dependency.version.jmockit>
        <dependency.version.javaxAPI>2.0</dependency.version.javaxAPI>
        <dependency.version.jersey>2.18</dependency.version.jersey>
        <dependency.version.servle3tAPI>1.0.2.Final</dependency.version.servle3tAPI>
        <dependency.version.inject>1</dependency.version.inject>
        <dependency.version.guava>18.0</dependency.version.guava>
        <dependency.version.reflections>0.9.9-RC1</dependency.version.reflections>
        <dependency.version.log4j>1.2.17</dependency.version.log4j>
        <dependency.version.slf4j>1.7.10</dependency.version.slf4j>
        <dependency.version.java-hamcrest>2.0.0.0</dependency.version.java-hamcrest>
        <dependency.version.hamcrest-junit>2.0.0.0</dependency.version.hamcrest-junit>
        <dependency.version.jackson>2.4.1</dependency.version.jackson>
        <dependency.version.javaeeAPI>7.0</dependency.version.javaeeAPI>
        <dependency.version.apisecurity>2.0.3</dependency.version.apisecurity>
        <dependency.version.jsr305>2.0.3</dependency.version.jsr305>
        <dependency.version.infinispan>6.0.2.Final</dependency.version.infinispan>
        <dependency.version.jetty>9.1.5.v20140505</dependency.version.jetty>
        
    </properties>

    <dependencies>
        
        <!-- Aspect -->
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjweaver</artifactId>
            <version>${dependency.version.aspectj}</version>
        </dependency>
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjrt</artifactId>
            <version>${dependency.version.aspectj}</version>
        </dependency>
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjtools</artifactId>
            <version>${dependency.version.aspectj}</version>
        </dependency>

        <!-- Commons -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${dependency.version.commonslang}</version>
        </dependency>
        <dependency>
            <groupId>commons-beanutils</groupId>
            <artifactId>commons-beanutils</artifactId>
            <version>${dependency.version.commonsbeanutils}</version>
        </dependency>

        <!-- Joda -->
		<dependency>
		    <groupId>joda-time</groupId>
		    <artifactId>joda-time</artifactId>
		    <version>${dependency.version.joda}</version>
		</dependency>

        <!-- Spring -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>
       <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-beans</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context-support</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-jdbc</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-orm</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>

        <!-- Spring Data JPA -->
        <dependency>
            <groupId>org.springframework.data</groupId>
            <artifactId>spring-data-jpa</artifactId>
            <version>${dependency.version.spring-data}</version>
        </dependency>
        
        <dependency>
	        <groupId>org.springframework.hateoas</groupId>
	        <artifactId>spring-hateoas</artifactId>
	        <version>0.19.0.RELEASE</version>
    	</dependency>

        <!-- Hibernate -->
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core</artifactId>
            <version>${dependency.version.hibernate}</version>
        </dependency>
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-entitymanager</artifactId>
            <version>${dependency.version.hibernate}</version>
        </dependency>
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-validator</artifactId>
            <version>${dependency.version.hibernate.validator}</version>
        </dependency>
        <dependency>
            <groupId>javax.el</groupId>
            <artifactId>javax.el-api</artifactId>
            <version>${dependency.version.javax.el.api}</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.web</groupId>
            <artifactId>javax.el</artifactId>
            <version>${dependency.version.javax.el}</version>
        </dependency>
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-validator-cdi</artifactId>
            <version>${dependency.version.hibernate.validator.cdi}</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jersey.ext</groupId>
            <artifactId>jersey-bean-validation</artifactId>
            <version>${dependency.version.jersey.bean.validation}</version>
        </dependency>

		<dependency>
			<groupId>org.codehaus.jackson</groupId>
			<artifactId>jackson-mapper-asl</artifactId>
			<version>1.9.13</version>
		</dependency>

		<!-- Insert contents of : pom_dependencies_service.txt -->

        <dependency>
			<groupId>org.modelmapper</groupId>
			<artifactId>modelmapper</artifactId>
			<version>0.7.5</version>
		</dependency>

        <!-- JDBC -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>5.1.37</version>
        </dependency>

        <!-- BoneCP -->
        <dependency>
            <groupId>com.jolbox</groupId>
            <artifactId>bonecp</artifactId>
            <version>${dependency.version.bonecp}</version>
        </dependency>

        <!-- Aspects -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-aop</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-aspects</artifactId>
            <version>${dependency.version.spring}</version>
        </dependency>


        <!-- Log4J -->

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>1.7.10</version>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-log4j12</artifactId>
            <version>1.7.10</version>
        </dependency>


        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjtools</artifactId>
            <version>1.8.4</version>
        </dependency>

        <!-- Profiling -->
        <dependency>
            <groupId>org.perf4j</groupId>
            <artifactId>perf4j</artifactId>
            <version>0.9.16</version>
        </dependency>

        <!-- HTTP Client -->
        <dependency>
            <groupId>org.apache.httpcomponents</groupId>
            <artifactId>httpclient</artifactId>
            <version>4.5.1</version>
        </dependency>

        <!-- Apache Commons -->
        <dependency>
            <groupId>commons-jexl</groupId>
            <artifactId>commons-jexl</artifactId>
            <version>1.1</version>
            <exclusions>
                <exclusion>
                    <artifactId>junit</artifactId>
                    <groupId>junit</groupId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
            <version>2.6</version>
        </dependency>
        <dependency>
            <groupId>commons-configuration</groupId>
            <artifactId>commons-configuration</artifactId>
            <version>1.10</version>
        </dependency>
        
        <dependency>
	        <groupId>br.com.store</groupId>
	        <artifactId>validation</artifactId>
	        <version>${dependency.version.validationAPI}</version>
        </dependency>

        <!-- JAVA EE -->
        <dependency>
            <groupId>javax</groupId>
            <artifactId>javaee-api</artifactId>
            <version>7.0</version>
        </dependency>
        <!-- Javax.WS.RS -->
        <dependency>
            <groupId>javax.ws.rs</groupId>
            <artifactId>jsr311-api</artifactId>
            <version>1.1.1</version>
        </dependency>

        <!-- Javax EL -->
        <dependency>
           <groupId>javax.el</groupId>
           <artifactId>javax.el-api</artifactId>
           <version>2.2.4</version>
        </dependency>
        <dependency>
           <groupId>org.glassfish.web</groupId>
           <artifactId>javax.el</artifactId>
           <version>2.2.4</version>
        </dependency>

        <!-- JACKSON -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
            <version>2.4.1</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.4.1</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <version>2.4.1</version>
        </dependency>

        <!-- Google -->
        <dependency>
           <groupId>com.google.code.gson</groupId>
           <artifactId>gson</artifactId>
           <version>2.2.4</version>
        </dependency>
        <dependency>
           <groupId>com.googlecode.json-simple</groupId>
           <artifactId>json-simple</artifactId>
           <version>1.1.1</version>
        </dependency>
        
        <!-- Spring Test -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>4.0.3.RELEASE</version>
        </dependency>

        <!-- TesteNG -->
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>6.8.7</version>
            <exclusions>
                <exclusion>
                    <artifactId>junit</artifactId>
                    <groupId>junit</groupId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Mockito -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>1.9.5</version>
            <scope>test</scope>
        </dependency>
		<dependency>
		    <groupId>org.powermock</groupId>
		    <artifactId>powermock-api-mockito</artifactId>
		    <version>1.5.5</version>
		    <scope>test</scope>
		</dependency>
		<dependency>
		    <groupId>org.powermock</groupId>
		    <artifactId>powermock-module-testng</artifactId>
		    <version>1.5.5</version>
		    <scope>test</scope>
		</dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-compat</artifactId>
            <version>3.2.1</version>
            <scope>test</scope>
        </dependency>

        <!-- Jetty -->
        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-server</artifactId>
            <version>9.1.5.v20140505</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-servlet</artifactId>
            <version>9.1.5.v20140505</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-webapp</artifactId>
            <version>9.1.5.v20140505</version>
            <scope>provided</scope>
        </dependency>

        <!-- JERSEY -->
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-bundle</artifactId>
            <version>1.17.1</version>
        </dependency>
    </dependencies>

    <build>
        <!-- Build Settings -->
        <!--  <finalName>${project.artifactId}-${project.version}</finalName>-->
        <finalName>${project.artifactId}</finalName>

        <!--
            This component is an implementation of Wagon provider for WebDAV server access.
        -->
        <extensions>
            <extension>
                <groupId>org.apache.maven.wagon</groupId>
                <artifactId>wagon-webdav-jackrabbit</artifactId>
                <version>2.4</version>
            </extension>
        </extensions>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <source>${jdk.version}</source>
                    <target>${jdk.version}</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>aspectj-maven-plugin</artifactId>
                <version>1.7</version>
                <configuration>
                    <complianceLevel>${jdk.version}</complianceLevel>
                </configuration>
                <executions>
                    <execution>
                    	<phase>process-sources</phase>
                        <goals>
                            <goal>compile</goal>
                            <goal>test-compile</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>org.aspectj</groupId>
                        <artifactId>aspectjtools</artifactId>
                        <version>1.8.4</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>

         <pluginManagement>
            <plugins>
                <!-- The Compiler Plugin is used to compile the sources of your project -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.1</version>
                    <configuration>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <source>${jdk.version}</source>
                        <target>${jdk.version}</target>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>aspectj-maven-plugin</artifactId>
                    <version>1.8.4</version>
                    <configuration>
                        <complianceLevel>${jdk.version}</complianceLevel>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>org.aspectj</groupId>
                            <artifactId>aspectjtools</artifactId>
                            <version>1.8.4</version>
                        </dependency>
                    </dependencies>
                    <executions>
                       <execution>
                           <goals>
                               <goal>compile</goal>
                               <goal>test-compile</goal>
                           </goals>
                       </execution>
                   </executions>
                </plugin>
            </plugins>
         </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>unit-test</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>2.17</version>
                        <configuration>
                            <argLine>${surefireArgLine}</argLine>
                            <skipTests>${skip.unit.tests}</skipTests>
                            <includes>
                                <include>**/*Test.java</include>
                            </includes>
                            <excludes>
                                <exclude>**/*Integration.java</exclude>
                            </excludes>
                        </configuration>
                        <executions>
                            <execution>
                                <id>unit-tests</id>
                                <phase>test</phase>
                                <goals>
                                    <goal>test</goal>
                                </goals>
                                <configuration>
                                    <includes>
                                        <include>**/*Test.java</include>
                                    </includes>
                                    <excludes>
                                        <exclude>**/*Integration.java</exclude>
                                    </excludes>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>0.7.0.201403182114</version>
                        
                        <executions>
                            <!--
                               Prepares the property pointing to the JaCoCo runtime agent which
                               is passed as VM argument when Maven the Surefire plugin is executed.
                           -->
                            <execution>
                                <id>pre-unit-test</id>
                                <goals>
                                    <goal>prepare-agent</goal>
                                </goals>
                                <configuration>
                                    <!-- Sets the path to the file which contains the execution data. -->
                                    <destFile>${sonar.jacoco.reportPath}</destFile>
                                    <!--
                                       Sets the name of the property containing the settings
                                       for JaCoCo runtime agent.
                                   -->
                                    <propertyName>surefireArgLine</propertyName>
                                </configuration>
                            </execution>
                            <!--
                               Ensures that the code coverage report for unit tests is created after
                               unit tests have been run.
                           -->
                            <execution>
                                <id>post-unit-test</id>
                                <phase>test</phase>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                                <configuration>
                                    <!-- Sets the path to the file which contains the execution data. -->
                                    <dataFile>${sonar.jacoco.reportPath}</dataFile>
                                    <!-- Sets the output directory for the code coverage report. -->
                                    <outputDirectory>${project.reporting.outputDirectory}/jacoco-ut</outputDirectory>
                                </configuration>
                            </execution>
                        </executions>

                    </plugin>
                </plugins>
            </build>

            <dependencies>
                <dependency>
                    <groupId>org.codehaus.sonar-plugins.java</groupId>
                    <artifactId>sonar-jacoco-listeners</artifactId>
                    <version>1.4</version>
                </dependency>
            </dependencies>
        </profile>

        <profile>
            <id>integration-test</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <configuration>
                            <argLine>${surefireArgLine}</argLine>
                            <reportsDirectory>${project.build.directory}/surefire-reports</reportsDirectory>
                            <skip>false</skip>
                            <includes>
                                <include>**/*Integration.java</include>
                            </includes>
                            <excludes>
                                <exclude>**/*Test.java</exclude>
                            </excludes>
                        </configuration>
                        <executions>
                            <execution>
                                <id>integration-test</id>
                                <phase>integration-test</phase>
                                <goals>
                                    <goal>integration-test</goal>
                                </goals>
                                <configuration>
                                    <includes>
                                        <include>**/*Integration.java</include>
                                    </includes>
                                    <excludes>
                                        <exclude>**/*Test.java</exclude>
                                    </excludes>
                                </configuration>
                            </execution>
                            <execution>
                                <id>verify</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>0.7.0.201403182114</version>
                       <executions>
                            <!-- The Executions required by unit tests are omitted. -->
                            <!--
                               Prepares the property pointing to the JaCoCo runtime agent which
                               is passed as VM argument when Maven the Failsafe plugin is executed.
                           -->
                            <execution>
                                <id>pre-integration-test</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>prepare-agent</goal>
                                </goals>
                                <configuration>
                                    <!-- Sets the path to the file which contains the execution data. -->
                                    <destFile>${sonar.jacoco.itReportPath}</destFile>
                                    <!--
                                       Sets the name of the property containing the settings
                                       for JaCoCo runtime agent.
                                   -->
                                    <propertyName>failsafeArgLine</propertyName>
                                </configuration>
                            </execution>
                            <!--
                               Ensures that the code coverage report for integration tests after
                               integration tests have been run.
                           -->
                            <execution>
                                <id>post-integration-test</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                                <configuration>
                                    <!-- Sets the path to the file which contains the execution data. -->
                                    <dataFile>${sonar.jacoco.itReportPath}</dataFile>
                                    <!-- Sets the output directory for the code coverage report. -->
                                    <outputDirectory>${project.reporting.outputDirectory}/jacoco-it</outputDirectory>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                </plugins>
            </build>
            <dependencies>
                <dependency>
                    <groupId>org.codehaus.sonar-plugins.java</groupId>
                    <artifactId>sonar-jacoco-listeners</artifactId>
                    <version>1.4</version>
                </dependency>
            </dependencies>
        </profile>

        <profile>
            <id>jmeter-tests</id>
            <build>
                <plugins>
                    <plugin>
						<groupId>com.lazerycode.jmeter</groupId>
						<artifactId>jmeter-maven-plugin</artifactId>
						<version>1.9.0</version>
                        <executions>
                            <execution>
                                <id>jmeter-tests</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>jmeter</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <reportDir>
                                ${project.reporting.outputDirectory}/jmeter
                            </reportDir>
                            <testResultsTimestamp>false</testResultsTimestamp>
                            <overrideRootLogLevel>DEBUG</overrideRootLogLevel>
                            <suppressJMeterOutput>false</suppressJMeterOutput>
                            <ignoreResultFailures>true</ignoreResultFailures>
                            <jmeterUserProperties>
                            </jmeterUserProperties>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>assembly</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                        <version>2.4</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                            </execution>
                        </executions>
                        <configuration>
                            <archive>
                                <addMavenDescriptor>false</addMavenDescriptor>
                            </archive>
                            <useDefaultManifestFile>true</useDefaultManifestFile>
                        </configuration>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-war-plugin</artifactId>
                        <version>2.3</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                            </execution>
                        </executions>
                        <configuration>
                            <archive>
                                <manifest>
                                    <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                                    <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                                </manifest>
                                <addMavenDescriptor>true</addMavenDescriptor>
                            </archive>
                            <archiveClasses>true</archiveClasses>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>deploy-stg</id>
            <build>
                <plugins>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-scm-plugin</artifactId>
                        <version>1.0</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <version>2.1</version>
                        <executions>
                            <execution>
                                <id>assembly</id>
                                <phase>package</phase>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/main/assembly/bin.xml</descriptor>
                                    </descriptors>
                                </configuration>
                                <goals>
                                   <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>uol.corp.plugins</groupId>
                        <artifactId>quebec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>runAssembly</goal>
                                </goals>
                                <configuration>
                                    <projectEmail>l-plataforma@uolinc.com
                                    </projectEmail>
                                    <assemblyId>package</assemblyId>
                                    <assemblyFormat>tar.gz</assemblyFormat>
                                    <waitForQuebecStatus>false</waitForQuebecStatus>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>1.6</version>
                        <executions>
                            <execution>
                                <id>upload</id>
                                <phase>deploy</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <!-- STG 1 -->
                                        <echo message="Iniciando entrega do pacote no servidor ${deploy.instance.1}" />

                                        <echo message="Removendo conteudo da pasta /tmp/ utilizando o usuario ${deploy.user}" />
                                        <sshexec trust="true" host="${deploy.instance.1}" username="${deploy.user}" password="${deploy.pass}" command="sudo rm -rf ~/tmp/*" />

                                        <echo message="Removendo pacote diretorio ${deploy.build}" />
                                        <sshexec trust="true" host="${deploy.instance.1}" username="${deploy.user}" password="${deploy.pass}" command="sudo rm -rf ${runtime.home}*" />

                                        <echo message="Copiando arquivo: ${project.build.directory}\${deploy.build}" />
                                        <scp trust="true" file="${project.build.directory}/${deploy.build}" remoteTofile="${deploy.user}:${deploy.pass}@${deploy.instance.1}:/home/${deploy.user}/tmp/" />

                                        <echo message="Movendo diretorio ${runtime.home}" />
                                        <sshexec trust="true" host="${deploy.instance.1}" username="${deploy.user}" password="${deploy.pass}" command="sudo mv /home/${deploy.user}/tmp/${deploy.build} /opt/UOLI/webapps/sac/" />

                                        <echo message="Instalando pacote /home/${deploy.user}/${deploy.build}" />
                                        <sshexec trust="true" host="${deploy.instance.1}" username="${deploy.user}" password="${deploy.pass}" command="sudo tar -zxvf /opt/UOLI/webapps/sac/${deploy.build} -C /" />
                                        
                                        <echo message="Criando link simb�lico do app-check" />
                                        <sshexec trust="true" host="${deploy.instance.1}" username="${deploy.user}" password="${deploy.pass}" command="sudo ln -f -s /opt/UOLI/webapps/app-check/app-check.war /opt/UOLI/webapps/sac/lib/app-check.war" />

                                        <echo message="Iniciando Jetty" />
                                        <sshexec trust="true" host="${deploy.instance.1}" username="${deploy.user}" password="${deploy.pass}" command="sudo /etc/init.d/jetty restart ${project.artifactId}" />
                                        
                                        <echo message="Entrega realizada no servidor ${deploy.instance.1}!!!" />

                                        <!-- STG 2 -->
                                        <echo message="Iniciando entrega do pacote no servidor ${deploy.instance.2}" />

                                        <echo message="Removendo conteudo da pasta /tmp/ utilizando o usuario ${deploy.user}" />
                                        <sshexec trust="true" host="${deploy.instance.2}" username="${deploy.user}" password="${deploy.pass}" command="sudo rm -rf ~/tmp/*" />

                                        <echo message="Removendo pacote diretorio ${deploy.build}" />
                                        <sshexec trust="true" host="${deploy.instance.2}" username="${deploy.user}" password="${deploy.pass}" command="sudo rm -rf ${runtime.home}*" />

                                        <echo message="Copiando arquivo: ${project.build.directory}\${deploy.build}" />
                                        <scp trust="true" file="${project.build.directory}/${deploy.build}" remoteTofile="${deploy.user}:${deploy.pass}@${deploy.instance.2}:/home/${deploy.user}/tmp/" />

                                        <echo message="Movendo diretorio ${runtime.home}" />
                                        <sshexec trust="true" host="${deploy.instance.2}" username="${deploy.user}" password="${deploy.pass}" command="sudo mv /home/${deploy.user}/tmp/${deploy.build} /opt/UOLI/webapps/sac/" />

                                        <echo message="Instalando pacote /home/${deploy.user}/${deploy.build}" />
                                        <sshexec trust="true" host="${deploy.instance.2}" username="${deploy.user}" password="${deploy.pass}" command="sudo tar -zxvf /opt/UOLI/webapps/sac/${deploy.build} -C /" />
                                        
                                        <echo message="Criando link simb�lico do app-check" />
                                        <sshexec trust="true" host="${deploy.instance.2}" username="${deploy.user}" password="${deploy.pass}" command="sudo ln -f -s /opt/UOLI/webapps/app-check/app-check.war /opt/UOLI/webapps/sac/lib/app-check.war" />

                                        <echo message="Iniciando Jetty" />
                                        <sshexec trust="true" host="${deploy.instance.2}" username="${deploy.user}" password="${deploy.pass}" command="sudo /etc/init.d/jetty restart ${project.artifactId}" />
                                        
                                        <echo message="Entrega realizada no servidor ${deploy.instance.2}!!!" />
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                        <dependencies>
                            <dependency>
                                <groupId>org.apache.ant</groupId>
                                <artifactId>ant-jsch</artifactId>
                                <version>1.8.1</version>
                            </dependency>
                            <dependency>
                                <groupId>com.jcraft</groupId>
                                <artifactId>jsch</artifactId>
                                <version>0.1.50</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
            <properties>
                <deploy.user>jenkins</deploy.user>
                <deploy.pass>jenkins</deploy.pass>
                <deploy.build>${project.artifactId}-package.tar.gz</deploy.build>
                <deploy.instance.1>d3-hubble-stg1</deploy.instance.1>
                <deploy.instance.2>d3-hubble-stg2</deploy.instance.2>
            </properties>
        </profile>

        <profile>
            <id>release</id>
            <build>
                <plugins>
                     <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-release-plugin</artifactId>
                        <version>2.3</version>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-scm-plugin</artifactId>
                        <version>1.9</version>
                        <configuration>
                            <providerImplementations>
                                <git>jgit</git>
                            </providerImplementations>
                        </configuration>
                        <dependencies>
                            <dependency>
                                <groupId>org.apache.maven.scm</groupId>
                                <artifactId>maven-scm-provider-jgit</artifactId>
                                <version>1.9</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <version>2.1</version>
                        <executions>
                            <execution>
                                <id>assembly</id>
                                <phase>package</phase>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/main/assembly/bin.xml</descriptor>
                                    </descriptors>
                                </configuration>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>uol.corp.plugins</groupId>
                        <artifactId>quebec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>runAssembly</goal>
                                </goals>
                                <configuration>
                                    <projectEmail>l-plataforma@uolinc.com
                                    </projectEmail>
                                    <assemblyId>package</assemblyId>
                                    <assemblyFormat>tar.gz</assemblyFormat>
                                    <waitForQuebecStatus>true</waitForQuebecStatus>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>1.6</version>
                        <executions>
                            <execution>
                                <id>upload</id>
                                <phase>deploy</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                                                       <target>                                        
                                        <echo message="Iniciando entrega do pacote no servidor ${deploy.instance}" />
                                        
                                        <echo message="Removendo pacote UOL${project.artifactId} do diretorio /home/${deploy.user}" />
                                        <sshexec trust="true" host="${deploy.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo rm -rf /home/${deploy.user}/UOL${project.artifactId}*" />

                                        <echo message="Baixando o arquivo rpm ${project.artifactId}-${project.version} para o servidor ${deploy.instance}" />
                                        <sshexec trust="true" host="${deploy.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo wget http://rpm.repo.intranet/so-centos/5.4/centos54_uol//noarch/UOL${project.artifactId}-${project.version}-1.noarch.rpm" />

                                        <echo message="Executando rpm -Uvh do arquivo ${project.artifactId}-${project.version}" />
                                        <sshexec trust="true" host="${deploy.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo rpm -Uvh UOL${project.artifactId}-${project.version}-1.noarch.rpm" />
                                        
                                        <echo message="Criando link simb�lico do app-check" />
                                        <sshexec trust="true" host="${deploy.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo ln -f -s /opt/UOLI/webapps/app-check/app-check.war /opt/UOLI/webapps/sac/lib/app-check.war" />

                                        <echo message="Iniciando Jetty" />
                                        <sshexec trust="true" host="${deploy.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo /etc/init.d/jetty restart ${project.artifactId}" />
                                        
                                        <echo message="Entrega realizada no servidor ${deploy.instance}!!!" />
                                        
                                        <echo message="Iniciando entrega do pacote no servidor ${deploy.hunf.instance}" />
                                        
                                        <echo message="Removendo pacote UOL${project.artifactId} do diretorio /home/${deploy.user}" />
                                        <sshexec trust="true" host="${deploy.hunf.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo rm -rf /home/${deploy.user}/UOL${project.artifactId}*" />
                    
                                        <echo message="Baixando o arquivo rpm ${project.artifactId}-${project.version} para o servidor ${deploy.hunf.instance}" />
                                        <sshexec trust="true" host="${deploy.hunf.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo wget http://rpm.repo.intranet/so-centos/5.4/centos54_uol//noarch/UOL${project.artifactId}-${project.version}-1.noarch.rpm" />

                                        <echo message="Executando rpm -Uvh do arquivo ${project.artifactId}-${project.version}" />
                                        <sshexec trust="true" host="${deploy.hunf.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo rpm -Uvh UOL${project.artifactId}-${project.version}-1.noarch.rpm" />

                                        <echo message="Criando link simb�lico do app-check" />
                                        <sshexec trust="true" host="${deploy.hunf.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo ln -f -s /opt/UOLI/webapps/app-check/app-check.war /opt/UOLI/webapps/sac/lib/app-check.war" />

                                        <echo message="Iniciando Jetty" />
                                        <sshexec trust="true" host="${deploy.hunf.instance}" username="${deploy.user}" password="${deploy.pass}" command="sudo /etc/init.d/jetty restart ${project.artifactId}" />
                                        
                                        <echo message="Entrega realizada no servidor ${deploy.hunf.instance}!!!" />
                                        
                                    </target>

                                </configuration>
                            </execution>
                        </executions>
                        <dependencies>
                            <dependency>
                                <groupId>org.apache.ant</groupId>
                                <artifactId>ant-jsch</artifactId>
                                <version>1.8.1</version>
                            </dependency>
                            <dependency>
                                <groupId>com.jcraft</groupId>
                                <artifactId>jsch</artifactId>
                                <version>0.1.50</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
            <properties>
                <deploy.user>jenkins</deploy.user>
                <deploy.pass>jenkins</deploy.pass>
                <deploy.build>${project.artifactId}-${project.version}-package.tar.gz</deploy.build>
                <deploy.instance>a1-deploycadf-s-pla1</deploy.instance>
                <deploy.hunf.instance>a1-hunf-q-pla1</deploy.hunf.instance>
            </properties>
        </profile>
    </profiles>

    <!-- Environment Settings -->
    <scm>
        <connection>scm:git:https://${username}:${password}@gituol.intranet/git/plataforma/sac/sac.git</connection>
        <developerConnection>scm:git:https://${username}:${password}@gituol.intranet/git/plataforma/sac/sac.git</developerConnection>
        <tag>sac-0.0.49</tag>
    </scm>

    <pluginRepositories>
        <pluginRepository>
            <id>central2</id>
            <name>Central Repository</name>
            <url>http://repo.maven.apache.org/maven2</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <updatePolicy>never</updatePolicy>
            </releases>
        </pluginRepository>
        <pluginRepository>
	    	<id>central</id>
	      	<name>Maven Plugin Repository</name>
	      	<url>http://repo1.maven.org/maven2</url>
	      	<layout>default</layout>
	      	<snapshots>
	        	<enabled>false</enabled>
	      	</snapshots>
	      	<releases>
	        	<updatePolicy>never</updatePolicy>
	      	</releases>
	    </pluginRepository>
    </pluginRepositories>

    <repositories>
        <repository>
            <id>jcenter.bintray.com</id>
           <name>bintray</name>
            <url>http://jcenter.bintray.com</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <enabled>true</enabled>
            </releases>
        </repository>
		<repository>
			<id>central</id>
		    <name>Maven Repository Switchboard</name>
		    <layout>default</layout>
		    <url>http://repo1.maven.org/maven2</url>
		    <snapshots>
		    	<enabled>false</enabled>
		    </snapshots>
		</repository>
        <repository>
            <id>repository.springframework.maven.release</id>
            <name>Spring Framework Maven Release Repository</name>
            <url>http://maven.springframework.org/release</url>
            <releases>
                <enabled>true</enabled>
                <updatePolicy>always</updatePolicy>
            </releases>
        </repository>
        
        <repository>
            <id>SRC</id>
            <url>http://mvn.intranet/uol/</url>
            <releases>
                <enabled>true</enabled>
                <updatePolicy>always</updatePolicy>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>3RD</id>
            <url>http://mvn.intranet/3rd/</url>
            <releases>
                <enabled>true</enabled>
                <updatePolicy>always</updatePolicy>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
       </repository>
        <repository>
            <id>Goldin lib central</id>
            <name>libs-releases</name>
            <url>http://evgenyg.artifactoryonline.com/evgenyg/libs-releases</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <distributionManagement>
        <repository>
            <id>uol</id>
            <name>UOL - Repositorio interno</name>
            <url>dav:https://upload.mvn.repo.intranet/uol</url>
        </repository>
        <snapshotRepository>
            <id>uol</id>
            <name>UOL - Repositorio interno</name>
            <url>dav:https://upload.mvn.repo.intranet/uol</url>
       </snapshotRepository>
    </distributionManagement>

</project>
